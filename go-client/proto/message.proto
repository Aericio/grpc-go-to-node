syntax = "proto3";
package proto;
option go_package = "github.com/aericio/grpc/go-client";

message CreateRequest {
  string key = 1;
  bytes value = 2;
}

message CreateResponse {
}

service Server {
  rpc Create(CreateRequest) returns (CreateResponse);
}